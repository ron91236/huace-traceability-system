#!/usr/bin/expect -f
set timeout 300

# Push to deploy remote
spawn git push deploy master
expect {
    "password:" {
        send "Rango2025=\r"
        exp_continue
    }
    "Everything up-to-date" {
        puts "Already up to date"
    }
    eof
}
